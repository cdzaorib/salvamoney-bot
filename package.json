{
  "name": "salvamoney-bot",
  "version": "2.0.1",
  "description": "Bot WhatsApp SalvaMoney via Evolution API, Firebase e Groq",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "find server.js src test -type f -name '*.js' -exec node --check {} \\;",
    "test": "node --test"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "firebase-admin": "^13.10.0",
    "groq-sdk": "^1.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=20"
  }
}
